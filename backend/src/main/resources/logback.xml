<configuration debug="true">
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Enable DEBUG logging for Solr queries to diagnose memory issues -->
    <logger name="uk.ac.ebi.spot.ols.repository.solr" level="debug"/>
    <logger name="uk.ac.ebi.spot.ols.controller.api.v1.V1SearchController" level="debug"/>
    <logger name="uk.ac.ebi.spot.ols.controller.api.v1.V1SelectController" level="debug"/>

    <!-- Keep general OLS logging at info -->
    <logger name="uk.ac.ebi.spot.ols" level="info"/>

    <root level="error">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>