apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    app: {{ .Release.Name }}-frontend
spec:
  ports:
      - port: 8081
        targetPort: 8081
        name: http
        protocol: TCP
  selector:
    app: {{ .Release.Name }}-frontend
