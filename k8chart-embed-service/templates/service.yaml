apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: NodePort
  ports:
  - name: "application"
    port: 5000
  selector:
    app: {{ .Release.Name }}
