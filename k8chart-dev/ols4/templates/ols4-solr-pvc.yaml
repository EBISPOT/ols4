apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ols4-solr-pvc
spec:
  storageClassName: local-storage
  volumeName: ols4-solr-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Gi

